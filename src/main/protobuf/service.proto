syntax = "proto3";

package reactive;

service ReactiveService {
  rpc send (stream ClientMessage) returns (stream ServerReply);
}

message ClientMessage {
  uint64 sequence = 1;
}

message ServerReply {
  uint64 sequence = 1;
}
